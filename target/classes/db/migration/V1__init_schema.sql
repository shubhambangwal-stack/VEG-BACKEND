-- ============================================================
-- VegGo Fresh Platform — Flyway Migration V1
-- Platform-level schema initialization placeholder
-- ============================================================
--
-- FLYWAY VERSION RANGE RESERVATION
-- =================================
-- Each module owns an exclusive range of Flyway version numbers.
-- Never use a version number outside your module's range.
-- Never reuse a version number, even if you delete a migration.
--
--   V1  – V19   → Platform Foundation    (this file and future platform tables)
--   V20 – V39   → Auth Module            (users, refresh_tokens, otp_codes, etc.)
--   V40 – V69   → Customer Module        (customer_profiles, addresses, etc.)
--   V70 – V89   → Vendor Module          (vendor_profiles, products, categories, etc.)
--   V90 – V109  → Delivery Module        (orders, delivery_agents, routes, etc.)
--   V110 – V129 → Admin Module           (admin_users, audit_logs, configs, etc.)
--   V130 – V149 → Payment Module         (transactions, wallets, payment_methods, etc.)
--   V150 – V169 → Notification Module    (notification_templates, push_tokens, etc.)
--
-- PLATFORM RESERVED RANGE (V1–V19)
-- ----------------------------------
-- V1  → This placeholder (reserved)
-- V2  → Reserved for platform-level audit or config table if needed in future
-- V3–V19 → Reserved for future platform infrastructure
--
-- ============================================================

-- Placeholder: No platform-level tables are required at Phase 0.
-- All module tables will be created in their respective version ranges.
-- This file intentionally contains no DDL statements.

-- (Reserved for future platform infrastructure tables)
